---
phase: 03-data-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/<timestamp>_usuario_email_nullable/migration.sql
  - src/app/api/membros/route.ts
  - src/app/api/membros/[id]/route.ts
  - src/lib/email.ts
  - src/lib/scheduler.ts
  - src/__tests__/api/membros.test.ts
  - src/__tests__/api/membros-id.test.ts
autonomous: true
must_haves:
  truths:
    - "Member creation and update accept missing email as null and never generate placeholder addresses."
    - "Database model allows Usuario records with null email while preserving uniqueness for non-null emails."
    - "Email-dependent flows fail safely when member email is missing instead of attempting delivery."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Nullable Usuario.email data model with preserved uniqueness semantics."
      contains: "email         String?    @unique"
    - path: "src/app/api/membros/route.ts"
      provides: "Admin member creation path without placeholder-email fallback."
      exports: ["POST"]
    - path: "src/app/api/membros/[id]/route.ts"
      provides: "Admin member update path that clears email to null when requested."
      exports: ["PATCH"]
    - path: "src/lib/scheduler.ts"
      provides: "Null-safe guards for email-dependent notification flows."
      contains: "if (!membro.usuario.email)"
  key_links:
    - from: "src/app/api/membros/route.ts"
      to: "prisma/schema.prisma"
      via: "nullable user email persistence in transaction create path"
      pattern: "email:\\s+normalizedEmail\\s*\\?\\?"
    - from: "src/app/api/membros/[id]/route.ts"
      to: "src/lib/email.ts"
      via: "email normalization and explicit null clear path"
      pattern: "normalizeEmail"
    - from: "src/lib/scheduler.ts"
      to: "src/lib/resend.ts"
      via: "guard before enviarEmail invocation when email missing"
      pattern: "enviarEmail"
---

<objective>
Implement context-locked Email-Missing behavior by removing placeholder-email strategy and making member email nullable end-to-end.

Purpose: Eliminate identity pollution from synthetic emails and enforce explicit guardrails for email-required operations.
Output: Nullable Prisma model + migrations, updated member create/update APIs, and null-safe email-dependent flow handling.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-integrity/03-CONTEXT.md
@.planning/phases/03-data-integrity/03-RESEARCH.md
@prisma/schema.prisma
@src/app/api/membros/route.ts
@src/app/api/membros/[id]/route.ts
@src/lib/email.ts
@src/lib/scheduler.ts
@src/__tests__/api/membros.test.ts
@src/__tests__/api/membros-id.test.ts
</context>

<tasks>

<task type="auto">
  <name>Make Usuario.email nullable with explicit Prisma migration</name>
  <files>prisma/schema.prisma, prisma/migrations/&lt;timestamp&gt;_usuario_email_nullable/migration.sql</files>
  <action>Update Prisma schema so `Usuario.email` is nullable (`String? @unique`) and generate an explicit migration SQL that drops NOT NULL safely without weakening non-null uniqueness guarantees. Keep migration deterministic and production-safe for deploy flow (`prisma migrate deploy`).</action>
  <verify>npm run db:generate && npm run typecheck</verify>
  <done>Prisma client types reflect nullable email and migration SQL is ready to apply in deploy environments.</done>
</task>

<task type="auto">
  <name>Remove placeholder email generation from member create/update APIs</name>
  <files>src/app/api/membros/route.ts, src/app/api/membros/[id]/route.ts, src/lib/email.ts, src/__tests__/api/membros.test.ts, src/__tests__/api/membros-id.test.ts</files>
  <action>Refactor member create (`POST /api/membros`) and update (`PATCH /api/membros/[id]`) to persist `null` when email is missing/cleared and to validate uniqueness only when a non-null email is provided. Remove every `temp_*@placeholder.local` fallback, keep generic user-facing error messaging, and update tests for null-email create/update contracts.</action>
  <verify>npm run test:run -- src/__tests__/api/membros.test.ts src/__tests__/api/membros-id.test.ts</verify>
  <done>Member create/update routes support null email without placeholders, and tests fail if placeholder generation is reintroduced.</done>
</task>

<task type="auto">
  <name>Guard email-dependent scheduler flows for null-email members</name>
  <files>src/lib/scheduler.ts</files>
  <action>Add explicit null-email guards in scheduler email send paths so flows are blocked/skipped safely until email exists, while preserving existing generic logging/error style. Ensure logic remains deterministic and does not throw on null email values introduced by this phase.</action>
  <verify>npm run typecheck</verify>
  <done>Scheduler email pathways are null-safe and no email send attempt occurs for members without email.</done>
</task>

</tasks>

<verification>
Run: `npm run test:run -- src/__tests__/api/membros.test.ts src/__tests__/api/membros-id.test.ts && npm run typecheck`
Confirm nullable email is persisted through member routes and no placeholder email generation remains in source.
</verification>

<success_criteria>
Context-locked DATA-02 interpretation is satisfied: missing email is modeled as null, placeholders are removed, and email-dependent flows are guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-integrity/03-02-SUMMARY.md`
</output>
