---
phase: 03-data-integrity
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - utility/migrate-placeholder-emails.ts
  - package.json
  - utility/logs/.gitkeep
autonomous: true
must_haves:
  truths:
    - "Existing placeholder emails are migrated to null using a deterministic, one-time script."
    - "Migration runs in preview and execute modes with pre-validation and fail-fast transactional behavior."
    - "Each migration run emits a JSON report artifact describing scanned, migrated, skipped, and failed counts."
  artifacts:
    - path: "utility/migrate-placeholder-emails.ts"
      provides: "Deterministic placeholder-email migration utility with preview and transactional execution modes."
      contains: "preview-only / execute mode handling and report serialization"
    - path: "package.json"
      provides: "Script entrypoints for running migration preview and execution."
      contains: "migrate:placeholder-emails"
    - path: "utility/logs/.gitkeep"
      provides: "Stable report artifact directory for migration outputs."
      contains: ""
  key_links:
    - from: "utility/migrate-placeholder-emails.ts"
      to: "prisma.usuario.updateMany"
      via: "transactional null-email migration for placeholder addresses"
      pattern: "updateMany"
    - from: "utility/migrate-placeholder-emails.ts"
      to: "utility/logs/"
      via: "JSON report output per run"
      pattern: "writeFileSync|writeFile"
    - from: "package.json"
      to: "utility/migrate-placeholder-emails.ts"
      via: "npm script command using tsx runtime"
      pattern: "tsx utility/migrate-placeholder-emails.ts"
---

<objective>
Deliver deterministic legacy cleanup for placeholder emails with reproducible evidence artifacts.

Purpose: Complete the context-locked data migration requirements safely and audibly after nullable-email model support exists.
Output: One-time migration utility with preview + execute modes, transactional fail-fast semantics, and run reports.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-integrity/03-CONTEXT.md
@.planning/phases/03-data-integrity/03-RESEARCH.md
@.planning/phases/03-data-integrity/03-01-SUMMARY.md
@.planning/phases/03-data-integrity/03-02-SUMMARY.md
@src/lib/email.ts
@utility/cleanup-inactive-members.ts
</context>

<tasks>

<task type="auto">
  <name>Implement deterministic preview pipeline for placeholder-email cleanup</name>
  <files>utility/migrate-placeholder-emails.ts</files>
  <action>Create a migration utility that identifies placeholder-style emails deterministically, performs pre-validation for the safe migration set, and supports preview mode (`--preview`) without mutating data. Include clear aggregate stats (total users scanned, candidates, safe set, blocked set) and generic logging for operational traceability.</action>
  <verify>npx tsx utility/migrate-placeholder-emails.ts --preview</verify>
  <done>Preview mode lists deterministic candidate/safe-set results and performs no database writes.</done>
</task>

<task type="auto">
  <name>Add transactional execute mode with fail-fast semantics and JSON report output</name>
  <files>utility/migrate-placeholder-emails.ts, utility/logs/.gitkeep</files>
  <action>Implement execute mode (`--execute`) that migrates only pre-validated safe records to null inside a single Prisma transaction (all-or-nothing). On completion (or failure), write a JSON report artifact under `utility/logs/` with timestamp, mode, candidate counts, migrated IDs, skipped IDs/reasons, and error metadata if execution aborts.</action>
  <verify>npx tsx utility/migrate-placeholder-emails.ts --execute --dry-run</verify>
  <done>Execute pipeline is transactional/fail-fast and produces a report artifact that can be reviewed after the run.</done>
</task>

<task type="auto">
  <name>Expose script entrypoints and confirm migration utility compiles cleanly</name>
  <files>package.json</files>
  <action>Add npm scripts for migration preview and execute modes (including a dry-run safety variant), then verify the utility compiles/runs with project tooling. Keep script naming explicit and scoped to one-time placeholder-email cleanup.</action>
  <verify>npm run typecheck && npm run migrate:placeholder-emails:preview</verify>
  <done>Migration commands are discoverable in package scripts and run without TypeScript/runtime errors.</done>
</task>

</tasks>

<verification>
Run: `npm run migrate:placeholder-emails:preview` and `npm run typecheck`
Before production execution, confirm preview safe-set counts and planned report path in `utility/logs/`.
</verification>

<success_criteria>
Legacy placeholder emails can be migrated to null through a deterministic, auditable, and transactional process with report artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-integrity/03-03-SUMMARY.md`
</output>
