---
phase: 03-data-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/anamnese.ts
  - src/app/api/minha-anamnese/route.ts
  - src/app/api/membros/[id]/anamnese/route.ts
  - src/app/api/anamnese-token/route.ts
  - src/__tests__/api/minha-anamnese.test.ts
  - src/__tests__/api/membros-id-anamnese.test.ts
  - src/__tests__/api/anamnese-token.test.ts
autonomous: true
must_haves:
  truths:
    - "The canonical anamnese field list is owned by src/lib/anamnese.ts and route files do not duplicate it."
    - "Unknown keys in anamnese payloads are ignored, while missing canonical keys are persisted as null."
    - "Anamnese read APIs auto-normalize malformed legacy records and persist normalization only when data changed."
  artifacts:
    - path: "src/lib/anamnese.ts"
      provides: "Canonical field registry plus sanitize/normalize helpers shared by all anamnese routes."
      exports: ["ANAMNESE_FIELDS", "sanitizeAnamnesePayload", "normalizeAnamneseRecord"]
    - path: "src/app/api/minha-anamnese/route.ts"
      provides: "Member self-service anamnese route wired to shared canonical sanitizer."
      exports: ["GET", "POST"]
    - path: "src/app/api/membros/[id]/anamnese/route.ts"
      provides: "Admin anamnese route with read-time normalization and persistence."
      exports: ["GET", "POST"]
    - path: "src/app/api/anamnese-token/route.ts"
      provides: "Token anamnese route with read-time normalization and persistence."
      exports: ["GET", "POST"]
  key_links:
    - from: "src/app/api/minha-anamnese/route.ts"
      to: "src/lib/anamnese.ts"
      via: "shared sanitize helper import"
      pattern: "sanitizeAnamnesePayload"
    - from: "src/app/api/membros/[id]/anamnese/route.ts"
      to: "src/lib/anamnese.ts"
      via: "normalize helper before response and conditional persistence"
      pattern: "normalizeAnamneseRecord"
    - from: "src/app/api/anamnese-token/route.ts"
      to: "src/lib/anamnese.ts"
      via: "normalize helper before response and conditional persistence"
      pattern: "normalizeAnamneseRecord"
---

<objective>
Deliver DATA-01 by consolidating anamnese field behavior to one canonical source and enforcing self-healing read/write integrity rules.

Purpose: Prevent field drift and legacy malformed payloads from persisting across anamnese entry points.
Output: Shared anamnese normalization helpers, route integrations, and regression tests for tolerant payload + self-healing behavior.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-integrity/03-CONTEXT.md
@.planning/phases/03-data-integrity/03-RESEARCH.md
@src/lib/anamnese.ts
@src/app/api/minha-anamnese/route.ts
@src/app/api/membros/[id]/anamnese/route.ts
@src/app/api/anamnese-token/route.ts
@src/__tests__/api/minha-anamnese.test.ts
@src/__tests__/api/membros-id-anamnese.test.ts
@src/__tests__/api/anamnese-token.test.ts
</context>

<tasks>

<task type="auto">
  <name>Promote src/lib/anamnese.ts to full canonical sanitize and normalize layer</name>
  <files>src/lib/anamnese.ts</files>
  <action>Refactor anamnese helper logic so canonical fields are centralized and reusable across routes: keep/introduce deterministic canonical field keys, update sanitize behavior to ignore unknown payload keys silently, default missing known keys to null, and expose a normalize helper for legacy records with explicit changed/no-changed metadata. Preserve generic integrity error contracts for unexpected normalization failures.</action>
  <verify>npm run test:run -- src/__tests__/api/membros-id-anamnese.test.ts src/__tests__/api/anamnese-token.test.ts</verify>
  <done>Anamnese helper module is the only canonical field source and supports tolerant write + self-healing read normalization behavior required by context decisions.</done>
</task>

<task type="auto">
  <name>Remove route-local ANAMNESE_FIELDS duplication from minha-anamnese route</name>
  <files>src/app/api/minha-anamnese/route.ts, src/__tests__/api/minha-anamnese.test.ts</files>
  <action>Delete local ANAMNESE_FIELDS/buildAnamneseData from `POST /api/minha-anamnese` and use shared `sanitizeAnamnesePayload` from `src/lib/anamnese.ts`. Ensure payload handling follows context rules (unknown keys ignored, missing canonical fields normalized to null) and keep existing onboarding completion + welcome email semantics. Update tests to assert canonical helper behavior through route responses/upsert payload shape.</action>
  <verify>npm run test:run -- src/__tests__/api/minha-anamnese.test.ts</verify>
  <done>`minha-anamnese` no longer defines its own field list and writes canonicalized data aligned with shared helper behavior.</done>
</task>

<task type="auto">
  <name>Add read-time self-healing normalization for admin and token anamnese endpoints</name>
  <files>src/app/api/membros/[id]/anamnese/route.ts, src/app/api/anamnese-token/route.ts, src/__tests__/api/membros-id-anamnese.test.ts, src/__tests__/api/anamnese-token.test.ts</files>
  <action>In both GET routes, normalize legacy/malformed anamnese payloads using the shared helper before returning data. Persist normalized data back to DB only when normalization produces changes; preserve existing generic client error surface and current auth/token behavior. Extend tests to cover normalization and persistence trigger/no-trigger scenarios, including tolerance for extra unknown keys.</action>
  <verify>npm run test:run -- src/__tests__/api/membros-id-anamnese.test.ts src/__tests__/api/anamnese-token.test.ts</verify>
  <done>Both read endpoints self-heal malformed persisted data and keep response contracts stable without exposing internals.</done>
</task>

</tasks>

<verification>
Run: `npm run test:run -- src/__tests__/api/minha-anamnese.test.ts src/__tests__/api/membros-id-anamnese.test.ts src/__tests__/api/anamnese-token.test.ts`
Confirm no route-local anamnese field duplication remains and read/write canonicalization follows context decisions.
</verification>

<success_criteria>
DATA-01 and context-specific integrity behaviors are satisfied: one canonical field source, tolerant write normalization, and persisted self-healing reads across anamnese routes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-integrity/03-01-SUMMARY.md`
</output>
