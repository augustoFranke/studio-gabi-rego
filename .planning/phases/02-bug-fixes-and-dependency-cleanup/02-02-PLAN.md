---
phase: 02-bug-fixes-and-dependency-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/membros/[id]/anamnese/route.ts
  - src/app/api/anamnese-token/route.ts
  - src/__tests__/api/membros-id-anamnese.test.ts
  - src/__tests__/api/anamnese-token.test.ts
autonomous: true
must_haves:
  truths:
    - "Both anamnese read endpoints return null for sexo when the database field is unset."
    - "Anamnese API responses never infer sexo from member names or suffix heuristics."
  artifacts:
    - path: "src/app/api/membros/[id]/anamnese/route.ts"
      provides: "Admin anamnese response contract with explicit-null sexo fallback."
      exports: ["GET", "POST"]
    - path: "src/app/api/anamnese-token/route.ts"
      provides: "Token anamnese response contract with explicit-null sexo fallback."
      exports: ["GET", "POST"]
    - path: "src/__tests__/api/membros-id-anamnese.test.ts"
      provides: "Regression coverage for sexo-null behavior in admin anamnese endpoint."
      contains: "GET returns null sexo when member.sexo is null"
    - path: "src/__tests__/api/anamnese-token.test.ts"
      provides: "Regression coverage for sexo-null behavior in token anamnese endpoint."
      contains: "GET returns null sexo when member.sexo is null"
  key_links:
    - from: "src/app/api/membros/[id]/anamnese/route.ts"
      to: "src/__tests__/api/membros-id-anamnese.test.ts"
      via: "GET response contract assertion"
      pattern: "member\.sexo.*null"
    - from: "src/app/api/anamnese-token/route.ts"
      to: "src/__tests__/api/anamnese-token.test.ts"
      via: "GET response contract assertion"
      pattern: "json\.sexo.*null"
---

<objective>
Fix BUG-02 by removing sexo heuristics from anamnese APIs and enforcing explicit null when sexo is unset.

Purpose: Eliminate incorrect inferred demographic data and keep endpoint behavior consistent across both anamnese entry points.
Output: Updated anamnese routes and tests for `/api/membros/[id]/anamnese` and `/api/anamnese-token` with null-only fallback.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bug-fixes-and-dependency-cleanup/02-RESEARCH.md
@src/app/api/membros/[id]/anamnese/route.ts
@src/app/api/anamnese-token/route.ts
@src/__tests__/api/membros-id-anamnese.test.ts
@src/__tests__/api/anamnese-token.test.ts
</context>

<tasks>

<task type="auto">
  <name>Remove sexo heuristic fallback from admin anamnese endpoint</name>
  <files>src/app/api/membros/[id]/anamnese/route.ts</files>
  <action>In `GET /api/membros/[id]/anamnese`, keep the existing mapped display values when `membro.sexo` is set, but return `null` when unset. Delete `determineSexo`, `FEMALE_NAMES`, and `FEMALE_ENDINGS` from this route so no name-based fallback remains.</action>
  <verify>npm run test:run -- src/__tests__/api/membros-id-anamnese.test.ts</verify>
  <done>The admin anamnese route returns `member.sexo: null` for unset sexo and contains no heuristic helper/constants.</done>
</task>

<task type="auto">
  <name>Remove sexo heuristic fallback from token anamnese endpoint</name>
  <files>src/app/api/anamnese-token/route.ts</files>
  <action>In `GET /api/anamnese-token`, replace `determineSexoEnum` fallback with direct DB-backed behavior (`membro.sexo ?? null`) and remove all heuristic constants/helpers from the file. Keep token validation, cookie handling, and POST behavior unchanged.</action>
  <verify>npm run test:run -- src/__tests__/api/anamnese-token.test.ts</verify>
  <done>The token endpoint returns `sexo: null` when unset and no longer includes name inference code paths.</done>
</task>

<task type="auto">
  <name>Update endpoint tests to enforce explicit-null sexo contract</name>
  <files>src/__tests__/api/membros-id-anamnese.test.ts, src/__tests__/api/anamnese-token.test.ts</files>
  <action>Replace heuristic-based expectations with explicit `null` assertions for missing sexo in both test suites. Retain existing positive cases where sexo is present and route status/error handling assertions.</action>
  <verify>npm run test:run -- src/__tests__/api/membros-id-anamnese.test.ts src/__tests__/api/anamnese-token.test.ts</verify>
  <done>Both anamnese GET test suites fail if either endpoint guesses sexo from names when DB sexo is null.</done>
</task>

</tasks>

<verification>
Run: `npm run test:run -- src/__tests__/api/membros-id-anamnese.test.ts src/__tests__/api/anamnese-token.test.ts`
Confirm both GET endpoints assert null sexo fallback and preserve existing success/error status behavior.
</verification>

<success_criteria>
BUG-02 is satisfied across both anamnese GET endpoints: missing sexo in DB yields null and no name-based gender inference remains.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-fixes-and-dependency-cleanup/02-02-SUMMARY.md`
</output>
