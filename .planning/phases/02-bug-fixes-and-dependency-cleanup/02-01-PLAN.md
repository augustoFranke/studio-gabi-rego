---
phase: 02-bug-fixes-and-dependency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/membros.ts
  - src/__tests__/actions/membros.test.ts
autonomous: true
must_haves:
  truths:
    - "After toggling or deleting a member, the /alunos page reflects the mutation through normal refresh flow without manual hard refresh."
    - "All member mutation server actions invalidate the canonical members route slug /alunos."
  artifacts:
    - path: "src/app/actions/membros.ts"
      provides: "Correct cache invalidation target for member mutation actions."
      exports: ["toggleMembroStatus", "deleteMembro", "deactivateMembro"]
    - path: "src/__tests__/actions/membros.test.ts"
      provides: "Regression assertions that member mutation actions revalidate /alunos."
      contains: "revalidatePath('/alunos') expectations for toggle/delete/deactivate"
  key_links:
    - from: "src/app/actions/membros.ts"
      to: "next/cache revalidatePath"
      via: "post-mutation cache invalidation call"
      pattern: "revalidatePath\\('/alunos'\\)"
    - from: "src/__tests__/actions/membros.test.ts"
      to: "src/app/actions/membros.ts"
      via: "action-level assertions for revalidation slug"
      pattern: "toHaveBeenCalledWith\\('/alunos'\\)"
---

<objective>
Fix BUG-01 by correcting member action cache invalidation to the real members page route.

Purpose: Prevent stale `/alunos` UI after member status and delete mutations.
Output: Updated member actions and regression tests enforcing `/alunos` revalidation across mutation paths.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bug-fixes-and-dependency-cleanup/02-RESEARCH.md
@src/app/actions/membros.ts
@src/__tests__/actions/membros.test.ts
</context>

<tasks>

<task type="auto">
  <name>Replace incorrect /membros revalidation targets with /alunos</name>
  <files>src/app/actions/membros.ts</files>
  <action>Update `toggleMembroStatus` and `deleteMembro` to call `revalidatePath('/alunos')` instead of `revalidatePath('/membros')`. Keep `deactivateMembro` on `/alunos` and do not change action return contracts or authorization logic from Phase 1.</action>
  <verify>npm run test:run -- src/__tests__/actions/membros.test.ts</verify>
  <done>All member mutation actions use `/alunos` as the invalidation target and no action keeps `/membros`.</done>
</task>

<task type="auto">
  <name>Add explicit revalidation assertions for toggle and delete flows</name>
  <files>src/__tests__/actions/membros.test.ts</files>
  <action>Extend the members action tests to assert `revalidatePath('/alunos')` is called in successful `toggleMembroStatus` and `deleteMembro` cases, matching the existing deactivate assertion. Keep unauthorized/error-path assertions intact.</action>
  <verify>npm run test:run -- src/__tests__/actions/membros.test.ts</verify>
  <done>Tests fail if any successful member mutation action revalidates a route other than `/alunos`.</done>
</task>

<task type="auto">
  <name>Verify no stale route slug remains in member actions</name>
  <files>src/app/actions/membros.ts</files>
  <action>Run a focused source check to ensure there are no remaining `revalidatePath('/membros')` calls in `src/app/actions/membros.ts` and rerun the members action suite for regression confidence.</action>
  <verify>! rg -n "revalidatePath\('/membros'\)" src/app/actions/membros.ts && npm run test:run -- src/__tests__/actions/membros.test.ts</verify>
  <done>Search returns no stale `/membros` revalidation call and members action tests pass.</done>
</task>

</tasks>

<verification>
Run: `npm run test:run -- src/__tests__/actions/membros.test.ts`
Confirm successful toggle/delete/deactivate tests all assert `revalidatePath('/alunos')`.
</verification>

<success_criteria>
BUG-01 is satisfied: member mutations invalidate `/alunos`, and the members list reflects status/delete changes without manual hard refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-fixes-and-dependency-cleanup/02-01-SUMMARY.md`
</output>
