---
phase: 02-bug-fixes-and-dependency-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
must_haves:
  truths:
    - "dompurify and isomorphic-dompurify are absent from production dependencies."
    - "pdf-lib exists only in devDependencies while PDF tests continue to run successfully."
    - "Dependency cleanup does not introduce build or test regressions."
  artifacts:
    - path: "package.json"
      provides: "Runtime vs test dependency boundary aligned with SEC-05."
      contains: "dompurify removed, isomorphic-dompurify removed, pdf-lib under devDependencies"
    - path: "package-lock.json"
      provides: "Resolved lockfile state matching updated dependency graph."
      contains: "npm lock entries consistent with package.json changes"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install/uninstall lockfile sync"
      pattern: "dompurify|isomorphic-dompurify|pdf-lib"
    - from: "package.json"
      to: "src/__tests__/pdf/generation.test.ts"
      via: "devDependency required by test import"
      pattern: "pdf-lib"
---

<objective>
Deliver SEC-05 by removing unused runtime dependencies and moving pdf-lib to devDependencies.

Purpose: Reduce production dependency surface while preserving test coverage that requires `pdf-lib`.
Output: Updated package manifest/lockfile and verification evidence that PDF tests and app build still pass.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bug-fixes-and-dependency-cleanup/02-RESEARCH.md
@package.json
@package-lock.json
@src/__tests__/pdf/generation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Audit repository imports for targeted dependency cleanup</name>
  <files>package.json</files>
  <action>Run focused import scans to confirm `dompurify` and `isomorphic-dompurify` are not used in runtime code and that `pdf-lib` is only used by tests/fixtures. Keep findings scoped to SEC-05 packages and avoid unrelated dependency churn.</action>
  <verify>rg -n "from ['\"]dompurify['\"]|from ['\"]isomorphic-dompurify['\"]|from ['\"]pdf-lib['\"]" src utility prisma</verify>
  <done>Audit confirms dompurify packages are safe to remove and pdf-lib remains needed for tests only.</done>
</task>

<task type="auto">
  <name>Update dependency manifest and lockfile to reflect SEC-05</name>
  <files>package.json, package-lock.json</files>
  <action>Use npm package commands to remove `dompurify` and `isomorphic-dompurify` from dependencies and relocate `pdf-lib` to devDependencies (not deleted). Ensure `package-lock.json` is updated in the same change and no unrelated package moves are introduced.</action>
  <verify>npm uninstall dompurify isomorphic-dompurify pdf-lib && npm install -D pdf-lib@^1.17.1</verify>
  <done>`package.json` has no dompurify packages in dependencies and lists `pdf-lib` only in devDependencies, with lockfile synchronized.</done>
</task>

<task type="auto">
  <name>Run regression checks for PDF test path and application build</name>
  <files>package.json</files>
  <action>Run targeted PDF generation tests and a full production build after dependency changes. If failures occur, fix only SEC-05-related issues before rerunning checks.</action>
  <verify>npm run test:run -- src/__tests__/pdf/generation.test.ts && npm run build</verify>
  <done>PDF generation tests pass and the build succeeds with cleaned dependency graph.</done>
</task>

</tasks>

<verification>
Run: `npm run test:run -- src/__tests__/pdf/generation.test.ts` and `npm run build`
Confirm `package.json` keeps `pdf-lib` only under `devDependencies` and omits `dompurify` + `isomorphic-dompurify` from `dependencies`.
</verification>

<success_criteria>
SEC-05 is satisfied: production dependencies no longer include dompurify packages, pdf-lib is dev-only, and dependency cleanup does not break the build or PDF test flow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-fixes-and-dependency-cleanup/02-03-SUMMARY.md`
</output>
