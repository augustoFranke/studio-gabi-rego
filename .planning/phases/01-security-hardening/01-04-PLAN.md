---
phase: 01-security-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/password-policy.schema.ts
  - src/schemas/auth.schema.ts
  - src/schemas/membro.schema.ts
  - src/app/api/auth/cadastro/route.ts
  - src/__tests__/schemas/auth.schema.test.ts
  - src/__tests__/schemas/membro.schema.test.ts
  - src/__tests__/api/auth-cadastro.test.ts
autonomous: true
must_haves:
  truths:
    - "Password validation rules are identical in registration and member update flows."
    - "Passwords lacking minimum length, uppercase letter, or number are rejected with one generic message."
    - "Invalid password submissions fail before mutation side effects are executed."
  artifacts:
    - path: "src/schemas/password-policy.schema.ts"
      provides: "Canonical password policy schema with generic error message."
      exports: ["passwordPolicySchema", "PASSWORD_POLICY_MESSAGE"]
    - path: "src/schemas/auth.schema.ts"
      provides: "Registration/auth schemas reusing canonical password policy."
      contains: "passwordPolicySchema"
    - path: "src/schemas/membro.schema.ts"
      provides: "Member update schema reusing canonical password policy for non-empty passwords."
      contains: "passwordPolicySchema"
  key_links:
    - from: "src/app/api/auth/cadastro/route.ts"
      to: "src/schemas/auth.schema.ts"
      via: "request validation boundary"
      pattern: "cadastroSchema"
    - from: "src/schemas/membro.schema.ts"
      to: "src/schemas/password-policy.schema.ts"
      via: "shared password rule import"
      pattern: "passwordPolicySchema"
---

<objective>
Unify password policy enforcement across registration and admin member update flows.

Purpose: Close SEC-04 by eliminating rule/message drift between auth and member schemas.
Output: Canonical shared password policy schema integrated into both flows with updated API + schema tests.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@src/schemas/auth.schema.ts
@src/schemas/membro.schema.ts
@src/app/api/auth/cadastro/route.ts
@src/__tests__/schemas/auth.schema.test.ts
@src/__tests__/schemas/membro.schema.test.ts
@src/__tests__/api/auth-cadastro.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create canonical shared password policy schema</name>
  <files>src/schemas/password-policy.schema.ts</files>
  <action>Define a single exported Zod schema for password policy (min 8 chars, at least one uppercase, at least one number) and one canonical error message string: "Password does not meet policy". Keep schema reusable for required and optional-password contexts.</action>
  <verify>pnpm vitest src/__tests__/schemas/auth.schema.test.ts src/__tests__/schemas/membro.schema.test.ts</verify>
  <done>Password policy and message are declared in one shared source file with stable exports for both schema modules.</done>
</task>

<task type="auto">
  <name>Integrate shared password policy in auth + member schemas and route validation</name>
  <files>src/schemas/auth.schema.ts, src/schemas/membro.schema.ts, src/app/api/auth/cadastro/route.ts</files>
  <action>Replace duplicated inline password rules in auth and member schemas with imports from the shared password policy module. Ensure member update keeps optional/empty-string semantics while enforcing policy whenever a non-empty password is provided. Update cadastro route to validate request password through schema boundary and return the same generic policy message on failure (remove per-rule custom messages).</action>
  <verify>pnpm vitest src/__tests__/api/auth-cadastro.test.ts src/__tests__/schemas/auth.schema.test.ts src/__tests__/schemas/membro.schema.test.ts</verify>
  <done>Registration and member update flows enforce identical policy and expose the same generic validation message.</done>
</task>

<task type="auto">
  <name>Update schema and API tests for policy consistency</name>
  <files>src/__tests__/schemas/auth.schema.test.ts, src/__tests__/schemas/membro.schema.test.ts, src/__tests__/api/auth-cadastro.test.ts</files>
  <action>Add/adjust tests to verify policy rejects weak passwords in both schema contexts and that API response for password-policy violations uses the canonical generic message. Keep assertions for all-or-nothing behavior by ensuring invalid passwords do not proceed to user creation/update side effects.</action>
  <verify>pnpm vitest src/__tests__/schemas/auth.schema.test.ts src/__tests__/schemas/membro.schema.test.ts src/__tests__/api/auth-cadastro.test.ts</verify>
  <done>Tests fail if either flow diverges from shared password rules or message contract.</done>
</task>

</tasks>

<verification>
Run: pnpm vitest src/__tests__/schemas/auth.schema.test.ts src/__tests__/schemas/membro.schema.test.ts src/__tests__/api/auth-cadastro.test.ts
Confirm weak passwords are uniformly rejected with the generic policy message in both registration and member update paths.
</verification>

<success_criteria>
SEC-04 is satisfied: one canonical password policy governs both registration and member update with consistent rejection messaging and behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-04-SUMMARY.md`
</output>
