---
phase: 01-security-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/security/cron-auth.ts
  - src/app/api/cron/cobrancas-whatsapp/route.ts
  - src/app/api/cron/tarefas-email/route.ts
  - src/__tests__/api/cron-cobrancas-whatsapp.test.ts
  - src/__tests__/api/cron-tarefas-email.test.ts
autonomous: true
must_haves:
  truths:
    - "Cron endpoints reject missing/malformed bearer tokens with HTTP 401 and generic Unauthorized message."
    - "Cron token validation is constant-time and not vulnerable to string-compare timing leaks."
    - "Cron auth failure logs are structured, minimal, and do not leak token values."
  artifacts:
    - path: "src/lib/security/cron-auth.ts"
      provides: "Centralized bearer token extraction and timingSafeEqual-based secret verification."
      exports: ["validateCronRequest"]
    - path: "src/app/api/cron/cobrancas-whatsapp/route.ts"
      provides: "Cron auth wired through shared validator before job execution."
      contains: "validateCronRequest"
    - path: "src/app/api/cron/tarefas-email/route.ts"
      provides: "Cron auth wired through shared validator before scheduler execution."
      contains: "validateCronRequest"
  key_links:
    - from: "src/app/api/cron/cobrancas-whatsapp/route.ts"
      to: "src/lib/security/cron-auth.ts"
      via: "shared auth validator"
      pattern: "validateCronRequest\(request"
    - from: "src/app/api/cron/tarefas-email/route.ts"
      to: "src/lib/security/cron-auth.ts"
      via: "shared auth validator"
      pattern: "validateCronRequest\(request"
---

<objective>
Centralize and harden cron endpoint authentication with constant-time secret validation.

Purpose: Close SEC-03 by removing direct string equality checks and enforcing one shared cron auth contract.
Output: Shared cron auth helper, both cron routes migrated, and regression tests covering unauthorized and valid token flows.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@src/app/api/cron/cobrancas-whatsapp/route.ts
@src/app/api/cron/tarefas-email/route.ts
@src/__tests__/api/cron-cobrancas-whatsapp.test.ts
@src/__tests__/api/cron-tarefas-email.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create shared cron token validator using timingSafeEqual</name>
  <files>src/lib/security/cron-auth.ts</files>
  <action>Implement centralized cron auth helper that extracts bearer token, handles missing/malformed token cases, validates secret presence, and compares token vs secret using crypto.timingSafeEqual with safe buffer-length handling. Return a typed result that routes can map to 401 Unauthorized on auth failures and log minimal structured warnings without token leakage.</action>
  <verify>pnpm vitest src/__tests__/api/cron-cobrancas-whatsapp.test.ts src/__tests__/api/cron-tarefas-email.test.ts</verify>
  <done>Validator is reusable by any cron route and provides a single secure comparison path for CRON_SECRET checks.</done>
</task>

<task type="auto">
  <name>Migrate both cron routes to shared validator</name>
  <files>src/app/api/cron/cobrancas-whatsapp/route.ts, src/app/api/cron/tarefas-email/route.ts</files>
  <action>Remove duplicated getToken logic and direct token !== secret checks in both cron routes. Call validateCronRequest at route start, return generic 401 Unauthorized for missing/malformed/invalid token outcomes, and preserve existing route business logic and success payloads for authorized executions.</action>
  <verify>pnpm vitest src/__tests__/api/cron-cobrancas-whatsapp.test.ts src/__tests__/api/cron-tarefas-email.test.ts</verify>
  <done>No cron route performs direct token string comparison, and both use centralized validation flow.</done>
</task>

<task type="auto">
  <name>Update cron endpoint tests for centralized auth semantics</name>
  <files>src/__tests__/api/cron-cobrancas-whatsapp.test.ts, src/__tests__/api/cron-tarefas-email.test.ts</files>
  <action>Strengthen tests to cover missing token, malformed authorization header, invalid token, and valid token paths for both routes. Ensure assertions remain generic on unauthorized responses and do not rely on internal helper implementation details.</action>
  <verify>pnpm vitest src/__tests__/api/cron-cobrancas-whatsapp.test.ts src/__tests__/api/cron-tarefas-email.test.ts</verify>
  <done>Tests fail if routes regress to permissive token parsing or non-generic unauthorized handling.</done>
</task>

</tasks>

<verification>
Run: pnpm vitest src/__tests__/api/cron-cobrancas-whatsapp.test.ts src/__tests__/api/cron-tarefas-email.test.ts
Confirm both cron routes deny malformed/missing tokens and still execute jobs for valid secrets.
</verification>

<success_criteria>
SEC-03 is satisfied: cron secret validation is centralized, constant-time, and consistently enforced across both cron endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-03-SUMMARY.md`
</output>
