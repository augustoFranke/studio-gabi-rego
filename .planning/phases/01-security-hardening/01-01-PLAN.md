---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/security/server-action-auth.ts
  - src/app/actions/membros.ts
  - src/__tests__/actions/membros.test.ts
autonomous: true
must_haves:
  truths:
    - "Authenticated members cannot execute toggleMembroStatus, deleteMembro, or deactivateMembro."
    - "Unauthorized action attempts return a generic Unauthorized message in a consistent { success, message } response shape."
    - "Forbidden attempts are logged with verbose server-side context without exposing sensitive data to clients."
  artifacts:
    - path: "src/lib/security/server-action-auth.ts"
      provides: "Reusable ADMIN-only server action guard with structured forbidden-attempt logging."
      contains: "requireAdminAction"
    - path: "src/app/actions/membros.ts"
      provides: "ADMIN-gated member mutation actions with standardized response contract."
      exports: ["toggleMembroStatus", "deleteMembro", "deactivateMembro"]
    - path: "src/__tests__/actions/membros.test.ts"
      provides: "Authorization and response-shape regression tests for protected server actions."
      contains: "unauthorized and admin success scenarios"
  key_links:
    - from: "src/app/actions/membros.ts"
      to: "src/lib/security/server-action-auth.ts"
      via: "top-of-action authorization guard"
      pattern: "requireAdminAction"
    - from: "src/app/actions/membros.ts"
      to: "src/__tests__/actions/membros.test.ts"
      via: "asserted action return contract"
      pattern: "\{ success: (true|false), message:"
---

<objective>
Harden member mutation server actions against role-based authorization bypass.

Purpose: Eliminate current SEC-01 exploit path where any authenticated session can call admin mutation actions.
Output: Shared server-action admin guard, updated member actions, and regression tests proving unauthorized calls are blocked.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@src/app/actions/membros.ts
@src/__tests__/actions/membros.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create shared ADMIN authorization guard for server actions</name>
  <files>src/lib/security/server-action-auth.ts</files>
  <action>Implement a reusable helper that reads server session via NextAuth auth(), verifies authenticated user with ADMIN role, and returns a typed result for allow/deny. On deny, log verbose structured context (event, action, actorId, actorRole, resourceId, reason) only on server logs and never expose these details to clients. Keep deny message generic as "Unauthorized" per locked decision.</action>
  <verify>pnpm vitest src/__tests__/actions/membros.test.ts</verify>
  <done>Helper exists and can be imported by server actions to make a single ADMIN check before Prisma mutations.</done>
</task>

<task type="auto">
  <name>Apply guard and standardize response shape in member actions</name>
  <files>src/app/actions/membros.ts</files>
  <action>Update toggleMembroStatus, deleteMembro, and deactivateMembro to execute guard-first and return early on deny. Standardize all success/failure returns to { success, message } (remove mixed error field usage), keep unauthorized message generic, and preserve existing mutation behavior for authorized ADMIN callers. Keep existing revalidation behavior unless current route is known broken by roadmap scope.</action>
  <verify>pnpm vitest src/__tests__/actions/membros.test.ts</verify>
  <done>All three actions are ADMIN-only and expose consistent return payloads for both deny and mutation outcomes.</done>
</task>

<task type="auto">
  <name>Add regression tests for authorization and response contract</name>
  <files>src/__tests__/actions/membros.test.ts</files>
  <action>Expand tests to mock auth states (ADMIN, MEMBRO, missing session) and assert that non-admin calls return { success: false, message: "Unauthorized" } without invoking Prisma writes. Update existing success/error assertions to use the standardized { success, message } shape and preserve behavior checks for status toggles, delete path, and deactivate path.</action>
  <verify>pnpm vitest src/__tests__/actions/membros.test.ts</verify>
  <done>Tests fail if any of the three actions execute for non-admin sessions or return non-standard payload keys.</done>
</task>

</tasks>

<verification>
Run: pnpm vitest src/__tests__/actions/membros.test.ts
Confirm unauthorized scenarios do not call prisma.membro.update or prisma.usuario.delete and always return generic Unauthorized.
</verification>

<success_criteria>
SEC-01 is satisfied for server actions: authenticated members cannot invoke protected admin mutations, and all action responses follow { success, message }.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-01-SUMMARY.md`
</output>
