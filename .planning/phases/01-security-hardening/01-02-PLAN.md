---
phase: 01-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/rate-limit.ts
  - src/__tests__/lib/rate-limit.test.ts
  - src/__tests__/api/auth-cadastro.test.ts
autonomous: true
must_haves:
  truths:
    - "In production, Redis/Upstash outages cause protected requests to be denied instead of allowed."
    - "Fail-closed limiter responses use the standardized payload { success: false, error: 'Rate limit unavailable' }."
    - "In development, limiter unavailability fails open with loud warning logs to preserve local workflow visibility."
  artifacts:
    - path: "src/lib/rate-limit.ts"
      provides: "Environment-aware fail-closed/fail-open limiter behavior contract."
      exports: ["rateLimitByIp", "isRateLimitConfigured"]
    - path: "src/__tests__/lib/rate-limit.test.ts"
      provides: "Unit coverage for production fail-closed and development fail-open branches."
      contains: "NODE_ENV-dependent assertions"
    - path: "src/__tests__/api/auth-cadastro.test.ts"
      provides: "Route-level assertion that limiter deny still maps to HTTP 429 block behavior."
      contains: "rate limit denies request"
  key_links:
    - from: "src/lib/rate-limit.ts"
      to: "src/app/api/auth/*/route.ts"
      via: "rateLimitByIp return object consumed by routes"
      pattern: "if \(!rateLimit\.success\)"
    - from: "src/lib/rate-limit.ts"
      to: "src/__tests__/lib/rate-limit.test.ts"
      via: "env branch and payload validation"
      pattern: "Rate limit unavailable"
---

<objective>
Enforce production fail-closed rate limiting with deterministic outage behavior.

Purpose: Close SEC-02 by preventing request bypass during Redis/Upstash outages while preserving explicit dev-mode diagnostics.
Output: Updated limiter contract plus tests proving production denial and standardized outage payload.
</objective>

<execution_context>
@/Users/augustodoregofranke/.codex/get-shit-done/workflows/execute-plan.md
@/Users/augustodoregofranke/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@src/lib/rate-limit.ts
@src/__tests__/api/auth-cadastro.test.ts
</context>

<tasks>

<task type="auto">
  <name>Implement environment-aware fail-closed limiter behavior</name>
  <files>src/lib/rate-limit.ts</files>
  <action>Update rateLimitByIp so production with unavailable limiter returns { success: false, error: "Rate limit unavailable" } instead of fail-open success. Keep test environment bypass unchanged. In development, preserve fail-open behavior but emit a loud warning log indicating limiter is not configured.</action>
  <verify>pnpm vitest src/__tests__/lib/rate-limit.test.ts</verify>
  <done>Production + missing limiter path always yields success=false with standardized error payload; development path still permits requests with warning.</done>
</task>

<task type="auto">
  <name>Add dedicated unit tests for limiter outage semantics</name>
  <files>src/__tests__/lib/rate-limit.test.ts</files>
  <action>Create/extend lib-level tests to cover: test env bypass, production fail-closed payload, development fail-open payload, and configured limiter happy path. Mock environment variables and header IP extraction behavior to keep assertions deterministic.</action>
  <verify>pnpm vitest src/__tests__/lib/rate-limit.test.ts</verify>
  <done>Tests fail if production ever returns success=true when limiter is unavailable or if standardized error message drifts.</done>
</task>

<task type="auto">
  <name>Validate route-level blocking contract remains intact</name>
  <files>src/__tests__/api/auth-cadastro.test.ts</files>
  <action>Update the existing rate-limit denial test to assert route response remains HTTP 429 when rateLimitByIp returns standardized fail-closed payload, ensuring protected endpoints keep blocking behavior under outage conditions.</action>
  <verify>pnpm vitest src/__tests__/api/auth-cadastro.test.ts</verify>
  <done>Auth signup route test proves limiter denial still blocks requests with 429 in API layer.</done>
</task>

</tasks>

<verification>
Run: pnpm vitest src/__tests__/lib/rate-limit.test.ts src/__tests__/api/auth-cadastro.test.ts
Confirm production outage branch cannot return success=true.
</verification>

<success_criteria>
SEC-02 is satisfied: production Redis/Upstash outages fail closed with standardized payload and protected auth routes remain blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-02-SUMMARY.md`
</output>
